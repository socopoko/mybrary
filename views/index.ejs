<h2>Recently Added</h2>
<div>
    <% books.forEach(book => { %>
        <a href="/books/<%= book.id %>">
            <img height="150"  src="<%= book.coverImagePath %>" alt="">
        </a>
<% }) %> 
</div>